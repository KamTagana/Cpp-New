#pragma once

namespace Lib 
{
	void PrintArray(const float*, const size_t);
	void PrintArray(const int*, const size_t);
	float* createAndIniArray(const size_t);
	void PrintPosAndNeg(const float*, const size_t);
	void SortArray(int*, const size_t);
}